// Placeholder module. The Absensi Dashboard uses `CategoryDistributionBar`
// (horizontal bar) for category breakdown, not a pie. This file exists only to
// clear stale VS Code phantom diagnostics — safe to delete once the Problems
// pane is empty.
export {}
